- Prefers white (`bg-white`) as the primary page/component background color over cream tones (e.g. `#FFFCF2`). Across multiple consecutive requests, the user is migrating the entire app from a cream background to white — dashboard, landing page, navbar, splash screen. Confidence: 0.85
- When making a styling change, expects the assistant to proactively scan and flag related surfaces (e.g. product cards, mobile drawer, dropdown hovers) that still use the old color, rather than waiting to be asked. Confidence: 0.75
- For copy that should be in front of the user, the company name **"PT Natura Inti Sukses"** is treated as the brand identity and must appear prominently on the hero/banner (e.g. as a "Welcome to PT Natura Inti Sukses" eyebrow). Confidence: 0.6
- Prefers full-bleed hero sections on landing pages: viewport-scale height (≈90vh with a min-h floor) **and** zero surrounding padding/margin (no `mx-auto max-w`, no `px-*`, no `pt-*` wrapper) so the hero spans edge-to-edge under the nav. Confidence: 0.75
- When fixing a reported bug, prefers scope-minimal changes: fix only what was asked (e.g. don't sweep in unrelated cleanup like unused imports in sibling files) and verify the fix with a typecheck/build before reporting done. Confidence: 0.7
- Prefers in-place "edit on the page" UX for admin content editing over a separate `/admin/content` form: when an admin is logged in and viewing a section (e.g. HeroSlider), show a small pencil/edit affordance directly on the page that opens a modal to edit that section's fields (per-language, saves to the i18n override store). The user explicitly rejected a working `/admin/content` form for the HeroSlider ("no I mean I don't wanna edit it from admin content but from landing page directly like there are pencil icon to edit it and will shows modal, only for HeroSlider"). Confidence: 0.9
- When the assistant implements the wrong UX (e.g. builds a form-based admin page when the user wanted in-place editing), expects a quick, clean revert + re-implementation rather than layering the new approach on top — the user will redirect with a short correction and expects the prior work to be undone. Confidence: 0.7
- When the user says "only for X", treat it as a strict scope signal: build the feature scoped to exactly that one section/component (e.g. only the HeroSlider pencil, not every section of the landing page), don't preemptively generalize. Confidence: 0.75
- Writes requests as informal narrative scenarios (e.g. "after login as admin and go back to landing page user can edit…") describing a user flow rather than a spec; expects the assistant to infer the underlying feature ask and implement it. Confidence: 0.75
- Does not want linting run as part of task verification — explicitly stated "don't do linting". Prefers verification via typecheck/build instead. Confidence: 0.8
- Expects a fix to be applied consistently across every analogous surface, including admin pages: after the public `/articles` page was wired to the API, the user added the terse follow-up "also fix it in admin articles" rather than restating the task, expecting the same data-loading fix mirrored to the admin articles list. Confidence: 0.65
- For waiting/loading states, prefers a skeleton placeholder component (shadcn-style) that mirrors the real layout, rather than briefly rendering stale seed/cached content — explicitly: "at /articles for waiting rendering use skeleton component". Confidence: 0.7
- Prefers content/sections be data-driven from the backend API rather than hardcoded or from a localStorage/seed fallback: when a section renders a static inline array of images/items, the user asks to "set it from the API", and when a list page shows only the hardcoded seed subset (e.g. "/articles" showing 3 instead of all 5 published) the user reports it as a bug expecting the page to fetch and render the full backend dataset, not the local cache. Confidence: 0.7
- Wants placeholder/dummy/seed data fully removed once real data exists — not just hidden or left as a fallback. Asked "remove social media dummy data" expecting the seeded backend rows deleted (while keeping the user's own real entries), the frontend seed array emptied, and stale cached dummy ids migrated out so they can't resurface. Confidence: 0.6
- Requires a confirmation dialog before every destructive admin action — stated as a blanket rule ("for every delete action should have confirmation to delete in admin"), so deletes must never fire immediately on click. Confidence: 0.85
- Iterates on visual placement through short directional nudges ("should be more bottom and left", then "yes, adjust a little bit up and left again"), expecting small incremental position tweaks; a terse "yes" means continue nudging, not finalize. Confidence: 0.6
- Cares about landing page performance and expects it to be checked/profiled proactively (e.g. asks to "check landing page performance" for `/`) rather than waiting for a reported slowness. Confidence: 0.55
- Wants page content editable end-to-end from the admin CMS: when asked for a settings page to change "all content" of a page (e.g. "add settings in admin to change all content in About"), expects every user-facing text string and image on that page to become an editable content key — including copy that was previously hardcoded — not just a subset, and a dedicated admin settings page (per-locale switch, add/remove/reorder lists, reset-to-default, save via the content API). This shows the earlier in-place pencil-editing preference was scoped to specific sections, not a blanket rule against admin form pages. Confidence: 0.6
- Prefers GitHub Actions as the CI/CD platform — explicitly asked to "setup ci/cd github actions workflow" for a Next.js + Docker project rather than another service. Confidence: 0.65
- When asking for infrastructure/tooling setup, issues very brief terse imperatives (e.g. "setup ci/cd github actions workflow") with no spec, and expects the assistant to inspect the repo, infer the stack/deploy approach, and ask clarifying questions before building. Confidence: 0.6
